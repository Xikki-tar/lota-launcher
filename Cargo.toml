[package]
name = "launcher_rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "launcher_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }

[profile.release]
strip = true
opt-level = 3
